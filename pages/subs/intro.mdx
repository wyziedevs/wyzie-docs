---
title: 'Intro to Wyzie Subs'
---

import { T, TP, TNote, TImportant } from '../../components/T';

# Intro to Wyzie Subs

<TP id="subs.intro.p1" />

<TNote id="subs.intro.note.ai" />

<TImportant id="subs.intro.important.apikey" />

<TNote id="subs.intro.note.npm" />

<div
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    gap: '0.5rem',
  }}
>
  <Button to="usage/package" type="secondary"><T id="subs.intro.btn.npm" /></Button>
  <Button to="usage/direct" type="secondary"><T id="subs.intro.btn.direct" /></Button>
</div>

## Protecting Your API Key

<TImportant id="subs.intro.protect.important" />

<TP id="subs.intro.protect.p1" />

- <T id="subs.intro.protect.item1" />
- <T id="subs.intro.protect.item2" />
- <T id="subs.intro.protect.item3" />
- <T id="subs.intro.protect.item4" />
- <T id="subs.intro.protect.item5" />

<TP id="subs.intro.protect.p2" />

### Option 1: Use Wyzie Worker

<TP id="subs.intro.protect.opt1.p1" />

```
Client → your-worker.workers.dev/search?id=286217 → sub.wyzie.io/search?id=286217&key=YOUR_KEY
```

### Option 2: Build Your Own Proxy

<TP id="subs.intro.protect.opt2.p1" />

```ts
// Example: a simple proxy endpoint
const API_KEY = process.env.WYZIE_API_KEY;

app.get("/subtitles", async (req, res) => {
  const params = new URLSearchParams(req.query);
  params.set("key", API_KEY);
  const response = await fetch(`https://sub.wyzie.io/search?${params}`);
  res.json(await response.json());
});
```
